

def is_ןidentity(mat):

    for i in range (len(mat)):

        for j in range (i + 1, len(mat)):
            if mat[i][j] != 0 or mat[j][i] != 0:
                return False
        for i in range (len(mat)):
            if mat[i][i] != 1:
                return False
    return True
